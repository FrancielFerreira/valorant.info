# Spec

A fonte canonica deste documento agora fica em `specs/001-initial/spec.md`.

Resumo: React 18, Vite, React Router DOM, Tailwind CSS e Valorant API publica em `pt-BR`, com rotas de home, listagens filtraveis/paginadas, detalhes de agentes, skins e sprays. Agentes usam `role.uuid` para filtro por funcao e skins usam `contentTierUuid` para filtro por edicao.
